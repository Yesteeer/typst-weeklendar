name: Build manual

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: typst-community/setup-typst@v3

      - run: |
          typst compile doc/examples/days-box.pdf
          typst compile doc/examples/debug.pdf
          typst compile doc/examples/empty.pdf
          typst compile doc/examples/event-box.pdf
          typst compile doc/examples/long-event.pdf
          typst compile doc/examples/multiple-days.pdf
          typst compile doc/examples/periodic-event.pdf
          typst compile doc/examples/single-event.pdf
          typst compile doc/examples/title-box.pdf
      - run: >-
          typst compile doc/manual.typ manual.pdf
          --root .
          --input source-url=${{ github.server_url }}/${{ github.repository }}/blob/${{ github.ref_name }}

      # Latest release (updated on every push to main)
      - name: Update latest release
        if: github.ref == 'refs/heads/master'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest (master)
          files: manual.pdf
          prerelease: true
          body: |
            Auto-updated from master branch.
            Commit: ${{ github.sha }}
